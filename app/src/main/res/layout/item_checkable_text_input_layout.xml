<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="56dp">

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/edit_layout"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:errorEnabled="true"
        app:layout_constraintEnd_toEndOf="@id/edit_layout_checkbox"
        app:layout_constraintStart_toStartOf="parent">

        <org.wildaid.ofish.ui.base.EmptyWarningEditText
            android:id="@+id/edit_layout_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:imeOptions="actionNext"
            android:paddingEnd="76dp"
            android:singleLine="true"
            tools:text="Hello world this is new wirdget for some inner purpos" />
    </com.google.android.material.textfield.TextInputLayout>

    <CheckBox
        android:id="@+id/edit_layout_checkbox"
        android:layout_width="68dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="None" />

</androidx.constraintlayout.widget.ConstraintLayout>